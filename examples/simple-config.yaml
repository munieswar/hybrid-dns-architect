# Simple DNS Configuration Example
# Uses only public DNS providers without complex routing

providers:
  - name: primary-dns
    type: public
    config:
      provider: google

  - name: backup-dns
    type: public
    config:
      provider: cloudflare

# No policies defined - will use all providers in order

cache:
  ttl: 600
